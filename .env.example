# 질병 아틀라스 — 환경변수 예시. 실제 값은 .env 에 두세요(.gitignore 처리됨).

# SQLite (개발). 배포(Vercel) 시 Postgres 연결 문자열로 교체.
DATABASE_URL="file:./dev.db"

# 관리자(/admin) 단일 비밀번호 — 반드시 강력한 값으로 변경.
ADMIN_PASSWORD="change-me"
